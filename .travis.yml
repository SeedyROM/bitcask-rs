sudo: true
language: rust
rust:
    - stable
    - 1.50.0  # Min Rust Version
    - beta
    - nightly

cache:
    cargo: true

# TODO: Push docs to GitHub pages

script:
- cargo build --verbose
- cargo test --verbose